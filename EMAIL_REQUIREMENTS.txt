# Email-related Python Requirements for Travel Companion

# Core Django email (comes with Django, no install needed)
Django>=3.2

# Recommended: For managing environment variables
python-decouple==3.8

# For production email service (optional upgrades from Gmail)
# Uncomment the one you want to use:

# SendGrid - Recommended for production
# sendgrid>=6.10.0
# sendgrid-django>=4.2.0

# Mailgun - Alternative production option
# mailgun>=1.0.2

# AWS SES - If you're using AWS
# django-ses>=3.0.0

# For async email sending (recommended for production)
# Celery and Redis
# celery>=5.3.0
# redis>=4.5.0

# For better email templates and styling
# django-anymail>=9.0
# premailer>=3.10.0

# Testing
# pytest-django>=4.5.0
# factory-boy>=3.2.0

# Note: Django comes with built-in SMTP support, no additional packages needed for basic Gmail setup

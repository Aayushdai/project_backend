import os; import django; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'travel_companion.settings'); django.setup(); from apps.trips.models import Trip; trips = Trip.objects.all().order_by('-id')[:10]; print('---LAST 10 TRIPS---'); [print(f'ID: {t.id} | Title: {t.title} | Trip Tags: {t.trip_tags} | Constraint Tags (count): {t.constraint_tags.count()}') for t in trips]; print('---END---')
